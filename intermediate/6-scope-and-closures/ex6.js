'use strict';

/*
Problem:
Implement makePrimitiveReader(value).

Requirements:
- get() must always return the original primitive value.
- set(next) must NOT affect the returned value.
- You must use closures; no global state.
*/

function makePrimitiveReader(value) {
  // TODO
}

module.exports = { makePrimitiveReader };
